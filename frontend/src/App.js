// React is intentionally inert; the birthday page is served as static HTML
// from public/index.html with its own scripts and localStorage state.
function App() {
  return null;
}

export default App;
